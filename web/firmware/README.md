# Web Flasher Artifacts

This directory contains the merged ESP32-S3 binary used by the browser flasher:

- `rsvp-nano.bin`

It is generated by:

```sh
python3 tools/export_web_firmware.py
```

The generated `.bin` file is intentionally ignored by Git. GitHub Pages builds and publishes it as part of the web flasher deploy.
